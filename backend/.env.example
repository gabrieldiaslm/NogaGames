# Local (Docker): docker compose up -d
DATABASE_URL="postgresql://nogagames:nogagames@localhost:5432/nogagames"
# Render: preenchido automaticamente pelo Blueprint (fromDatabase)

JWT_SECRET="troque-por-um-segredo-forte"
JWT_EXPIRES_IN="7d"
RAWG_KEY="sua-chave-da-rawg"
PORT=3333
FRONTEND_ORIGIN="http://localhost:5173"